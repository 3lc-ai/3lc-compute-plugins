# 3LC Compute plugin manifest — the single source of truth for this plugin's
# metadata. The host loads the plugin via runtime.entrypoint; there is no
# register() call at import and no metadata on the plugin class.
id = "timm"
name = "timm"
description = "Fine-tune any of 1,000+ pretrained image classifiers from timm (PyTorch Image Models) with per-sample metrics and embedding collection."
version = "1.0.0"
min_service_version = "0.1.0"
icon = "◆"
icon_svg = '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="2" width="12" height="12" rx="2"/><path d="M2 6h12M6 2v12"/></svg>'

[ui]
display_mode = "sidebar"
section = "AI Tools"
compatible_with = ["table"]
input_types = ["table", "table"]
output_types = ["run"]
min_input_count = 1
action_param_names = ["train", "val"]
quick_action = true
quick_action_label = "Train with timm"
quick_action_description = "Fine-tune a timm image classifier on your data with embeddings"

[runtime]
isolation = "venv"
entrypoint = "tlc_plugin_timm:TimmPlugin"
# Heavy stack lives behind this extra of the providing distribution; the host
# installs it ONLY into this plugin's provisioned venv, never the host venv.
provision_extra = "timm"
requires_gpu = true
training = true
